export const APP_CONFIG = {
  name: import.meta.env.VITE_APP_NAME || "Miw's Book",
  API_URL: import.meta.env.VITE_API_URL || "http://localhost:8000/api/v1",
  // add other global config values here
};
