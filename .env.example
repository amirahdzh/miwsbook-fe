# Copy this file to `.env` and fill values for local development.
# Vite requires client-side variables to be prefixed with VITE_

# API base URL (no trailing slash)
# Example local: http://localhost:8000/api/v1
VITE_API_URL=

# App name (optional)
VITE_APP_NAME=Miw's Book

# Any other environment-specific keys your app needs can be added here
